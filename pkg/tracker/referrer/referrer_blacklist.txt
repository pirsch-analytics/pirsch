# This file contains all substrings (in lowercase) used to filter the referrer header.
# Please add the reference in case you copy an existing list.
# Don't forget to update the array by running scripts/update_referrer_blacklist/update_referrer_blacklist.go!

# Custom (last update: 2024-09-05)
select(
from(
sleep(
for(
while(
select (
from (
sleep (
for (
while (
" or "
" and "
" xor "
pg_sleep
sysdate
