# This file contains all substrings (in lowercase) used to filter the referrer header.
# Please add the reference in case you copy an existing list.
# Remember to update the array by running scripts/update_referrer_blacklist/update_referrer_blacklist.go!

# Custom (last update: 2025-08-17)
select(
from(
sleep(
for(
while(
select (
from (
sleep (
for (
while (
" or "
" and "
" xor "
pg_sleep
sysdate
content-type
/bin/cat
/etc/passwd
